info:
  name: health-check
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVICE_URL}}/api/v1/orders/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
